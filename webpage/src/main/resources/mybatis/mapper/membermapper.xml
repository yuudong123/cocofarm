<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cocofarm.webpage.mapper.MemberMapper">

<select id="login" resultType="com.cocofarm.webpage.domain.MemberVO">
select * from TBL_MEMBER where EMAIL = #{email} and PASSWORD = #{password}
</select>

<insert id="join">
insert into TBL_MEMBER (member_no, member_type_cd, email, password, nickname, joindate, isactivated)
values (seq_member_no.nextval, 102, #{email}, #{password}, #{nickname}, sysdate, 'Y')
</insert>

<update id="am_modify">
update tbl_member
set nickname = #{nickname}, password = #{password}, phonenumber = #{phonenumber}
where email = #{email}
</update>

</mapper>